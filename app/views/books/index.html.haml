-#%h1.tekst-1strona
-#  Książki:
- for book in @books 
  .post
  %h2.title
    = link_to book.title, book_path(book)
    %p.tekst-1strona
      Autor: #{book.author}
      %br
      Cena: #{book.price}

