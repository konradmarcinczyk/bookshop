%h2
  Edycja konta
= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put }) do |f|

  .clearfix
    = f.label :email, t(:email).capitalize
    .input= f.email_field :email
  .clearfix
    = f.label :password, t(:password).capitalize
    .input
      = f.password_field :password
      %span.help-block
        Pozostaw puste, jeśli nie chcesz zmieniać hasła.
  .clearfix
    = f.label :password_confirmation, t(:password_confirmation).capitalize
    .input= f.password_field :password_confirmation
  .clearfix
    = f.label :current_password, t(:current_password).capitalize
    .input
      = f.password_field :current_password
      %span.help-block
        Potwierdź zmiany hasłem.
  .clearfix.center= f.submit t(:sign_up), :class => "btn primary"

%h3 Usuwanie konta
Kliknij #{link_to "tutaj", registration_path(resource_name), :confirm => "Jesteś pewien, że chcesz usunąć konto?", :method => :delete}, aby usunąć konto.
%br/
%br/
= link_to t(:back), :back


